replicaCount: 2

broker:
  replicaCount: 2
  persistence:
    enabled: false     # ✅ 브로커 PVC 생성 방지

auth:
  clientProtocol: plaintext
  interBrokerProtocol: plaintext

zookeeper:
  enabled: true
  replicaCount: 3
  persistence:
    enabled: false

persistence:
  enabled: false

volumePermissions:
  enabled: false

config: |
  log.retention.hours=168
  offsets.topic.replication.factor=2
  transaction.state.log.replication.factor=2
  transaction.state.log.min.isr=1

service:
  type: ClusterIP

controller:
  replicaCount: 0

kraft:
  enabled: false
